# Task 7 Report

Status: DONE (implementation and focused/full plugin gates passed)

## Commit

- `23d2f04 feat(codex): package Forge as a managed plugin`

## Files changed

- `.chezmoiscripts/run_onchange_after_install-codex-forge.sh.tmpl`
- `dot_local/share/codex-forge-marketplace/dot_agents/plugins/marketplace.json`
- `dot_local/share/codex-forge-marketplace/plugins/codex-forge/dot_codex-plugin/plugin.json`
- `dot_local/share/codex-forge-marketplace/plugins/codex-forge/README.md`
- `tests/test_codex_forge_plugin.sh`

## Implementation

- Added `local-managed` marketplace manifest with local `./plugins/codex-forge` source, Productivity category, AVAILABLE installation policy, and ON_INSTALL authentication policy.
- Added native plugin manifest for `codex-forge` version `0.1.0`, Forge skills, and hook bundle.
- Added run-on-change installer using exactly the native marketplace-add and plugin-add commands; it has `set -euo pipefail`, preserves unrelated Codex configuration through native CLI behavior, and embeds the plugin version in its content hash.
- Added operator documentation for install/update, `/hooks` trust, `$forge`, nonce approval choices, direct/Ralph status, cancellation, and workflow-enforcement threat boundary.
- Added temporary HOME/CODEX_HOME integration fixture. It renders the installer, installs twice through native Codex, verifies cache manifest/skill/hooks, enabled version, marketplace registration, unrelated config tables, and idempotent plugin listing.

## Tests

Passed:

- `bash tests/test_codex_forge_plugin.sh`
- `python3 -m unittest discover -s dot_local/share/codex-forge-marketplace/plugins/codex-forge/tests -p 'test_*.py' -v` (97 tests)
- `python3 tests/test-public-safety.py` (48 tests)
- `bash tests/test-compose.sh`
- `bash tests/test_codex_forge_ralph.sh`
- `scripts/chezmoi-compose preflight personal`
- `scripts/chezmoi-compose preflight work`
- `git diff --check` and both JSON manifests via `python3 -m json.tool`

Clean-clone scanner command was run against `git archive HEAD`; it reported pre-existing baseline `private-email` findings in `dot_local/share/codex-forge-marketplace/plugins/codex-forge/tests/test_ralph.py` and `tests/test_codex_forge_ralph.sh`. Those files were not changed by Task 7. The working-tree scanner also saw pre-existing ignored `.pi-subagents/` and review artifacts; none were staged.

## Scope and safety

No overlay changes, live HOME/CODEX_HOME mutation, Task 8 work, apply, or push. Pre-existing untracked `.pi-subagents/` was not modified or staged. Report intentionally remains outside the Task 7 commit.

## Review fixes

- Removed scanner-triggering email literals from Ralph fixtures; Git identities now use the opaque `forge-test` value. Marketplace-qualified selectors are assembled at runtime using a shell-generated at-sign.
- Installer fixture now parses the source plugin manifest and rendered installer version, requiring exact equality and `0.1.0`; either side changing alone fails.

Commands/results:

- `bash tests/test_codex_forge_plugin.sh` — passed.
- `bash tests/test_codex_forge_ralph.sh` — passed.
- `python3 -m unittest discover -s dot_local/share/codex-forge-marketplace/plugins/codex-forge/tests -p 'test_*.py'` — passed (97 tests).
- `python3 tests/test-public-safety.py` — passed (48 tests).
- `bash tests/test-compose.sh` — passed.
- `git diff --check` — passed.
- `python3 scripts/check-public-safety.py` on the working tree — blocked by pre-existing ignored `.pi-subagents/` artifacts, `.superpowers/sdd/review-*.diff` artifacts, `.git`, and generated bytecode; no changed public test source findings remain.
- `fixture=$(mktemp -d); git archive HEAD | tar -x -C "$fixture"; git diff --binary HEAD | git -C "$fixture" apply --binary; python3 "$fixture/scripts/check-public-safety.py"` — passed with no findings.
